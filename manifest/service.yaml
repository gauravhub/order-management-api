apiVersion: v1
kind: Service
metadata:
  name: order-management-api
  namespace: default
  labels:
    app: order-management-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: order-management-api
